services:
  app:
    image: ghcr.io/OWNER/solostore:latest
    restart: unless-stopped
    labels:
      pi.ui.primary: "true"
    ports:
      - "127.0.0.1:18080:8080"
    environment:
      - PORT=8080
